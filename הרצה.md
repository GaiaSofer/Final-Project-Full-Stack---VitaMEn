# איך מריצים את VitaMEn

## פעם אחת בלבד

### 1. התקנת החבילות
פותחים טרמינל בתיקייה הזו ומריצים:

    npm install

### 2. מפתח Supabase
פותחים את הקובץ `.env.local` שנמצא כאן בתיקייה:

    open -e .env.local

כתובת הפרויקט כבר ממולאת. צריך להדביק רק את המפתח:
Supabase → Project Settings → API → מעתיקים את `anon public`
ומדביקים במקום `YOUR-ANON-KEY-HERE`. שומרים (Cmd+S).

### 3. מסד הנתונים
ב-Supabase → SQL Editor. לכל קובץ: מוחקים הכל בעורך (Cmd+A),
מדביקים את **תוכן** הקובץ, ולוחצים Run. לפי הסדר הזה:

    supabase/migrations/0001_init.sql
    supabase/migrations/0002_v1_features.sql
    supabase/migrations/0003_auth_trigger.sql
    supabase/migrations/0004_grants_fix.sql
    supabase/migrations/0005_catalog_indexes.sql
    supabase/migrations/0006_tips.sql
    supabase/migrations/0007_audit_log.sql
    supabase/migrations/0008_audit_log_fix.sql
    supabase/migrations/0009_audit_log_admin_default.sql
    supabase/migrations/0010_supplement_constraints.sql
    supabase/seed.sql

⚠️ **אם כבר הרצת migrations קודם (למשל רק עד 0004):** חובה להריץ גם את
0005–0010 עכשיו, גם אם ה-DB "עובד" — בלעדיהם הקטלוג לא מדופדף/מסונן
כמו שצריך, אין טבלת טיפים, אין יומן פעולות אדמין (הוא יכשל בשקט —
ראה `docs/submission/05_security.md` לפירוט), ואין הגנה מפני מוצר עם
מחיר מקסימום נמוך ממחיר מינימום או שורות רכיב כפולות.

לפתיחת קובץ להעתקה, בטרמינל:  `open supabase/migrations/0001_init.sql`

### 4. כיבוי אישור מייל
Supabase → Authentication → Sign In / Providers → Email →
לכבות את "Confirm email" → Save.

## הרצה יומיומית

    npm run dev

ואז לפתוח בדפדפן: http://localhost:3000

לעצירת השרת: Ctrl+C  (לא Ctrl+Z — זה משאיר אותו תקוע ברקע)

## בדיקה שהכול עובד

1. Sign up עם סוג חשבון **מנהל** → אמור להיפתח מסך ניהול הקטלוג עם 14 מוצרים.
2. התנתקות → Sign up עם מייל אחר וסוג חשבון **לקוח**.
3. כלקוח: קטלוג → להוסיף לרשימה גם Omega-3 וגם Vitamin D3.
4. במסך "הרשימה שלי" אמורה להופיע אזהרה צהובה על חפיפת ויטמין D.

אם שלב 4 עובד — המוצר עובד מקצה לקצה.

## בדיקות אוטומטיות

    npm test

אמורות לעבור 35 בדיקות (מנוע הבטיחות, גאיגי, ניתוב לפי role, הרשאות על /catalog, פאג'ינציה ב-/today).
